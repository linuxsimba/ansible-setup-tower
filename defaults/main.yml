---
# defaults file for ansible-setup-tower

ansible_tower_version: 'latest'
base_url_ansible_tower: 'http://releases.ansible.com/ansible-tower/setup/ansible-tower-setup'
download_setup_bundle: false
postgres_streaming_replication: true
tower_install_url: "{{ base_url_ansible_tower}}-{{ansible_tower_version}}.tar.gz"

