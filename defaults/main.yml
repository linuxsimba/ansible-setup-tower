---
# defaults file for ansible-setup-tower
# Set this to true if using the Ansible tower bundle
use_bundle: false
bundle_var: ''
postgres_streaming_replication: false
postgres_replication_repo: "https://github.com/skamithi/ansible-role-postgresql-replication/tarball/master"
